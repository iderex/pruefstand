# report

The report card assembly. It reads result records and writes the published
artefacts: the page a person reads, the data a person redraws, and the
provenance that ties every number back to the command that produced it.

It computes nothing it could instead read from a record. A number that first
appears here is a number with no run behind it, and the report card exists to
carry numbers that have one.
